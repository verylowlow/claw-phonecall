这是一份为你整理的《基于 Openclaw 与 Root 安卓手机的 AI 电话 Agent 技术方案需求说明书（PRD）》。你可以直接保存此文档作为后续开发的指导准则。



\---



\# 项目需求说明书：基于 Openclaw 的实体手机 AI 通话 Agent



\## 1. 项目概述

\### 1.1 项目目标

利用已 Root 的安卓手机作为物理拨号终端，通过 USB 与本地 PC 连接，在 Openclaw 框架下实现一个具备 ASR、TTS、VAD 及拟人化交互能力的 AI 电话 Agent。



\### 1.2 核心痛点

\* \*\*成本控制：\*\* 避开高额的 VoIP 及第三方语音流服务费用。

\* \*\*高可靠性：\*\* 利用真实 SIM 卡号码拨打，降低封号风险，提高接通率。

\* \*\*低延迟交互：\*\* 追求接近真人通话的响应速度（目标 800ms 以内）。



\---



\## 2. 总体架构设计

方案采用\*\*“硬隔离音频环回 + ADB 指令控制”\*\*的软硬件结合架构。







\### 2.1 硬件链路（方案 B：物理隔离）

通过物理线缆实现 PC 与手机的双向音频传输，规避安卓系统复杂的音频权限协议栈。

\* \*\*TTS 路径（PC -> 手机）：\*\* PC USB -> 外置声卡 -> 隔离器 -> 手机麦克风输入。

\* \*\*ASR 路径（手机 -> PC）：\*\* 手机耳机输出 -> 对录线 -> 外置声卡麦克风输入 -> PC 处理。

\* \*\*控制路径：\*\* PC -> USB 数据线 -> ADB 指令 -> 手机系统。



\### 2.2 软件技术栈

\* \*\*控制层：\*\* Python + ADB (Android Debug Bridge)。

\* \*\*VAD (端点检测)：\*\* Silero VAD (本地轻量化)。

\* \*\*ASR (语音识别)：\*\* 火山ASR服务或者Faster-Whisper (流式推理)。

\* \*\*LLM (大脑)：\*\* 接入 Openclaw 默认大模型。

\* \*\*TTS (语音合成)：\*\* Edge-TTS (极低延迟) 或 火山TTS或GPT-SoVITS (高度拟人)。



\---



\## 3. 功能需求详细说明



\### 3.1 通话控制 (Telephony Control)

\* \*\*拨号：\*\* 通过 ADB 触发 `am start` 指令。

\* \*\*接听：\*\* 监控系统状态，识别响铃后模拟 `input keyevent 5`。

\* \*\*状态监控：\*\* 实时轮询 `dumpsys telephony.registry` 获取 `mCallState`。

&#x20;   \* `0`: 闲置；`1`: 响铃；`2`: 通话中。

\* \*\*挂断：\*\* 任务完成或对方挂断后，执行 `input keyevent 6`。



\### 3.2 语音处理流水线 (Pipeline)

\* \*\*流式 ASR：\*\* 边听边转文字，无需等待用户说完。

\* \*\*打断机制：\*\* 当 VAD 再次检测到用户说话（User Barge-in），立即清空当前 TTS 输出缓冲区。

\* \*\*拟人化填充（Filler）：\*\* \* 当 VAD 检测到静音且 LLM 尚未返回结果时，在 1.2 秒时自动插入“嗯”、“好的”、“我在听”等短促音效。



\### 3.3 低延迟优化

\* \*\*音频重采样：\*\* 全链路采用 16kHz 采样率，减少计算量。

\* \*\*模型预热：\*\* 在拨号/响铃阶段提前加载模型进内存。

\* \*\*并发处理：\*\* 采用多线程隔离 ASR 监听、TTS 播报与 ADB 状态监控。



\---



\## 4. 硬件清单 (BOM)



| 组件名称 | 推荐规格 | 数量 | 关键作用 |

| :--- | :--- | :--- | :--- |

| \*\*USB 外置声卡\*\* | 免驱版 (CM108) | 1 | 提供 PC 音频输入输出接口 |

| \*\*3.5mm 一分二线\*\* | TRRS 转 双 TRS | 1 | 拆分手机耳机与麦克风通道 |

| \*\*共地噪声隔离器\*\* | 3.5mm 接口 | 1 | \*\*核心：\*\* 消除 USB 共地产生的电流干扰声 |

| \*\*3.5mm 对录线\*\* | 公对公 | 2 | 连接声卡与适配线 |

| \*\*Type-C 转接头\*\* | 支持通话/充电二合一 | 1 | 解决手机充电与音频占位冲突 |



\---



\## 5. Openclaw Skill 逻辑逻辑图



```python

class PhoneAgentSkill:

&#x20;   def \_\_init\_\_(self):

&#x20;       self.state = "IDLE" # 状态机：IDLE, DIALING, RINGING, ACTIVE

&#x20;       

&#x20;   def monitor\_phone(self):

&#x20;       # 后台线程循环检测 mCallState

&#x20;       

&#x20;   def on\_incoming\_call(self):

&#x20;       # 识别来电，延时 2 秒接听

&#x20;       

&#x20;   def process\_audio(self):

&#x20;       # Silero VAD -> Faster-Whisper -> LLM -> TTS -> USB Out

```



\---



\## 6. 后续待研究课题

1\.  \*\*回音消除 (AEC)：\*\* 物理环回方案可能导致对方听到自己的回声，需在 PC 端尝试软件级双向抵消。

2\.  \*\*号码识别：\*\* 从 `logcat` 中实时解析来电号码并匹配客户画像。

3\.  \*\*异常处理：\*\* 如通话中信号中断、ADB 连接掉线的自动恢复机制。



在硬件准备就绪的同时，我们来攻克\*\*接听逻辑\*\*。



对于已 Root 的安卓手机，接听电话的逻辑分为两步：\*\*实时监控（监听到有电话拨入）\*\*和\*\*指令执行（自动接起）\*\*。



\---

\#openclaw agent接听电话方案



\## 一、 如何识别有电话拨入？



在 Openclaw 的 Skill 中，我们需要一个后台监听进程（Monitor）。这里有三种主流方案，按稳定性从高到低排列：



\### 1. 方案 A：ADB `dumpsys` 状态轮询 (最简单)

安卓系统会将通话状态实时记录在 `telephony.registry` 中。

\* \*\*命令：\*\* `adb shell dumpsys telephony.registry | grep mCallState`

\* \*\*状态码说明：\*\*

&#x20;   \* `mCallState=0`: 闲置 (IDLE)

&#x20;   \* `mCallState=1`: \*\*响铃 (RINGING)\*\* -> 此时有电话拨入！

&#x20;   \* `mCallState=2`: 通话中 (OFFHOOK)



\*\*逻辑实现：\*\* 在 Python 中启动一个死循环，每隔 500ms 执行一次该命令。一旦检测到状态从 `0` 变为 `1`，立即触发接听流程。



\### 2. 方案 B：ADB `logcat` 实时流监控 (延迟最低)

响铃时，系统日志会疯狂刷屏。我们可以通过监听特定关键词来捕获事件。

\* \*\*命令：\*\* `adb shell logcat -b radio | grep "RING"`

\* \*\*优点：\*\* 响应速度比轮询快，基本是毫秒级。

\* \*\*获取来电号码：\*\* 通过 `logcat` 还可以解析出 `incoming number`，让你的 Agent 在接听前就知道对方是谁（例如：“你好，张总”）。



\### 3. 方案 C：Tasker / MacroDroid 联动 (最稳定)

在手机上安装 Tasker（需要 Root 权限）。

\* \*\*配置：\*\* 设置一个触发器“来电时” -> 执行动作“发送 HTTP 请求”或“写入某个本地文件”。

\* \*\*优点：\*\* 这种方式最省电，且由于是系统原生广播触发，成功率接近 100%。



\---



\## 二、 如何自动接听电话？



一旦识别到 `mCallState=1`，Openclaw 需要发送接听指令。



\### 1. 标准模拟按键指令

这是最通用的方法，模拟用户按下了耳机上的“接听”键：

\* \*\*命令：\*\* `adb shell input keyevent 5`

\* \*\*注意：\*\* 部分高版本安卓系统为了安全，可能禁用了 `input keyevent` 在锁屏状态下的接听功能。如果失效，需要先执行一次 `adb shell input keyevent 26`（唤醒屏幕）。



\### 2. Service Call 指令 (底层接听)

如果模拟按键不起作用，可以使用更底层的系统调用：

\* \*\*命令：\*\* `adb shell service call phone 1` (具体数字 `1` 可能随安卓版本变化，通常为 1-5 之间)。



\---



\## 三、 完整的接听流程设计



当电话接入时，Openclaw 的 `PhoneAgentSkill` 应该执行以下序列：







1\.  \*\*检测响铃：\*\* 监控发现 `mCallState=1`。

2\.  \*\*提取信息：\*\* 从 `dumpsys` 或 `logcat` 提取来电号码（可选）。

3\.  \*\*执行接听：\*\* 发送 `adb shell input keyevent 5`。

4\.  \*\*确认通话建立：\*\* 循环检查 `mCallState` 直到变为 `2`。

5\.  \*\*启动 AI 管道：\*\*

&#x20;   \* \*\*第一句话策略：\*\* 为了降低延迟，接通后的 0.5 秒内，Agent 应立即通过 TTS 播放一句预设好的欢迎语（如：“喂，您好，请问哪位？”）。

&#x20;   \* \*\*加载 VAD/ASR：\*\* 在播放欢迎语的同时，后台启动音频采集，进入“听取”状态。



\---



\## 四、 特殊场景处理



\* \*\*拒绝来电：\*\* 如果来电号码在黑名单，执行 `adb shell input keyevent 6` (挂断)。

\* \*\*静音接听：\*\* 刚接通时，先保持 PC 端输出静音 200ms，避开接通瞬间的电流杂音。

\* \*\*通话状态同步：\*\* 如果对方挂断了，`mCallState` 会变回 `0`，此时 Skill 必须立即停止所有 ASR/TTS 进程，释放硬件占用，回到 `IDLE` 待命。



\---



\## 专家建议：



为了让 Openclaw 更像“人”，我建议在识别到响铃后，\*\*不要秒接\*\*。

让手机\*\*响铃 2-3 秒\*\*后再接听，这样对方的心理预期会更自然，同时也给了本地 ASR 和 TTS 模型预热（Warm-up）的时间。

